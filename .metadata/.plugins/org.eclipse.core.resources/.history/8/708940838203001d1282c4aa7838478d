package chess;

public class ChessPosition {
	
	private char column;
	private char row;
	public ChessPosition(char column, char row) {
		if (column < 'a' || column > 'h' || row <1 || row > 8);
		this.column = column;
		this.row = row;
	}
	
	
	
}
